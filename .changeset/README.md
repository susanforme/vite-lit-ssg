# Changesets

This directory stores release intent files for `vite-plugin-lit-ssg`.

## Create a changeset

```bash
pnpm changeset
```

Pick `vite-plugin-lit-ssg`, choose the correct semver bump, and write a short summary.

After your PR merges to `main`, the release workflow will open or update the release PR automatically.
